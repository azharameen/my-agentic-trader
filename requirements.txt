# =============================================================================
# NIFTY 100 Swing Trading Assistant — Python dependencies (Python 3.11+)
# =============================================================================

# --- Core data & math -------------------------------------------------------
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.40

# --- LLM (qualitative research filter) -------------------------------------
langchain>=0.2.0
langchain-openai>=0.2.0

# --- LangGraph (state machine, checkpoints, HITL interrupts) ---------------
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0

# --- Configuration ----------------------------------------------------------
pydantic>=2.5.0
pydantic-settings>=2.1.0

# --- Human-in-the-Loop (Telegram) ------------------------------------------
python-telegram-bot[job-queue]>=21.0

# --- Scheduling (daily EOD job + monthly universe refresh) -----------------
apscheduler>=3.10.0

# --- Resilience (retry/backoff for network + LLM calls) ---------------------
tenacity>=8.2.0

# --- News ingestion (RSS headlines for the catalyst analyst) ---------------
feedparser>=6.0.10

# --- HTTP (universe CSV fetch, Telegram Bot API fallback) -------------------
requests>=2.31.0

# --- Observability ----------------------------------------------------------
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0

# --- Testing (dev only) ------------------------------------------------------
pytest>=8.0.0
pytest-mock>=3.12.0
responses>=0.25.0
